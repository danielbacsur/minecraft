{
  "name": "minecraft",
  "version": "0.0.0",
  "private": true,

  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },

  "devDependencies": {
    "prettier": "^3",
    "turbo": "^2"
  },

  "overrides": {
    "drizzle-orm": "1.0.0-rc.4"
  },

  "engines": {
    "node": "^26"
  },

  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "^26"
    },
    "packageManager": {
      "name": "npm",
      "version": "^12"
    }
  },

  "workspaces": [
    "apps/minecraft-frontend",
    "packages/minecraft-auth",
    "packages/minecraft-postgres"
  ]
}
